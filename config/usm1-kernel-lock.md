# usm1 Kernel Lock Configuration

**Host:** usm1.9xc.local (172.16.254.231)  
**Date:** 2026-03-05 04:04 UTC  
**Status:** Active ✅

## Purpose

Lock usm1 kernel to version 6.5.13-5-pve to maintain NVIDIA VGPU driver compatibility.

## Applied Configuration

### APT Pinning

**File:** `/etc/apt/preferences.d/99-proxmox-kernel-pin`

```
# Pin Proxmox kernel to 6.5.13-5 to maintain NVIDIA VGPU compatibility
# Created: 2026-03-05 03:59 UTC

Package: linux-image-*
Pin: release o=Proxmox
Pin-Priority: 1001

Package: linux-headers-*
Pin: release o=Proxmox
Pin-Priority: 1001

Package: linux-modules-*
Pin: release o=Proxmox
Pin-Priority: 1001
```

### Monitoring Script

**File:** `/usr/local/bin/check-kernel-version.sh`

```bash
#!/bin/bash
EXPECTED_KERNEL="6.5.13-5-pve"
CURRENT_KERNEL=$(uname -r)
LOG_FILE="/var/log/kernel-check.log"
TIMESTAMP=$(date '+%Y-%m-%d %H:%M:%S')

if [ "$CURRENT_KERNEL" != "$EXPECTED_KERNEL" ]; then
    echo "[$TIMESTAMP] ALERT: Kernel version mismatch on $(hostname)" >> "$LOG_FILE"
    echo "  Expected: $EXPECTED_KERNEL" >> "$LOG_FILE"
    echo "  Current:  $CURRENT_KERNEL" >> "$LOG_FILE"
    echo "  Host:     $(hostname)" >> "$LOG_FILE"
    exit 1
fi
exit 0
```

**Permissions:** `755`

### Cron Schedule

**File:** `/etc/cron.d/kernel-check`

```
# Daily kernel version check for usm1
0 6 * * * root /usr/local/bin/check-kernel-version.sh
```

## Verification

**Current kernel:** `6.5.13-5-pve` ✓

**Sudo access:** hal-maint user has passwordless sudo ✓

## Related Documentation

- MEMORY.md - Long-term memory constraint
- ITIL-CHANGE-USM1-KERNEL-CONSTRAINT.md - Change request
- usm2.9xc.local - Different host, no kernel constraint

## Rollback

If kernel update occurs:
1. Reboot to previous kernel from GRUB
2. Reinstall NVIDIA VGPU drivers
3. Reapply APT pinning configuration

---
*Auto-generated by Sam (ocg) - 2026-03-05 04:04 UTC*