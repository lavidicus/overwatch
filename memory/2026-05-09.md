# 2026-05-09 Daily Log

## Session: 02:58 UTC — MTP Performance Tuning + USM1 Storage Setup

### MTP Speculative Drafting Optimization (pve3090-111)
- **Problem:** llama.cpp on pve3090-111 running Qwen3.6-27B-Q8_0 with MTP showed low generation speed (40.7 tok/s) and poor draft acceptance rate (45.3%) with `--spec-draft-n-max 5`
- **Root cause:** 5 draft tokens per step was too aggressive — drafts 4 and 5 were mostly low-confidence predictions getting rejected, burning GPU cycles
- **Fix:** Changed `--spec-draft-n-max 5` → `--spec-draft-n-max 3` in `/etc/systemd/system/llama-server.service`
- **Result:** Acceptance rate improved to 78-100%, generation speed improved to 51-65 tok/s, draft gen time dropped from 4077ms → 391-629ms
- **Service file path:** `/etc/systemd/system/llama-server.service` on pve3090-111

### USM1 — New PVE Host Setup
- **Host:** USM1 (`ssh root@usm1`, IP resolves internally)
- **Hardware:** 4× 3.6TB drives in ZFS pool `store` (14TB raw, ~11TB usable)
- **Action:** Changed ZFS mountpoint from `/store` → `/export/storage`
  - `zfs set mountpoint=/export/storage store`
  - `zfs mount store`
  - Verified: 11TB available at `/export/storage`
- **Purpose:** Bulk storage for LXC containers (specifically mas LXC #102)

### LXC 102 (mas.9xc.local) Storage Planning
- Container config at `/etc/pve/lxc/102.conf` on USM1
- Needs `/export/storage` bind-mounted as `mp0`
- Command needed: `pct set 102 -mp0 /export/storage,mp=/export/storage`
- **Not yet applied** — waiting for Jeremy to confirm

### TS Storage Context
- FAST (zfspool: fast-store) and mas (lvm: RAID) are TS-local storage
- Not available on USM1 because they're node-local (ZFS pool + LVM VG exist only on TS hardware)
- USM1 has its own storage: `local`, `local-zfs` (rpool/data), `store`

### Obsidian Vault
- Jeremy asked about Obsidian vault notes — no vault access established yet
- Need to locate vault and set up access

### Pending
- [ ] Apply mp0 bind mount to LXC 102 on USM1
- [ ] Locate and integrate Obsidian vault
- [ ] Complete USM1 LXC provisioning
