# 2026-05-15

## Systems Status Sweep (17:28 UTC)
- Full infra audit completed
- **Reachable:** claw, node1, node2, pve3090-111, hermes, TS, USM1, n8n, gog-bridge ✅
- **Unreachable:** dell (SSH timeout), home-server (DNS resolution failure) ❌

## LLM Benchmark (17:45 UTC)
- Ran speed check on node1 and node2 (both Qwen3.6-35B Q4_K_M, 2× P6000)
- **Node1:** 53.5 tok/s gen, 190.3 tok/s prompt | 2.96s total
- **Node2:** 53.1 tok/s gen, 228.6 tok/s prompt | 2.52s total
- Streaming: ~25 tok/s (SSE overhead expected)
- Both identical HW, identical performance. Model is GPU-bound.

## Optimization Applied (17:50-17:55 UTC)
- Updated llama-server.service on BOTH node1 and node2:
  - `--poll 2048` (was default 50) — main GPU speed knob
  - `--ctx-size 32768` (was 262144/131072) — freed VRAM on KV cache
  - `--tensor-split 0.5,0.5` (was unset) — explicit dual-GPU split
  - `--threads 32 --threads-batch 32` (was 14) — better CPU utilization
  - `--mlock` — prevent swap
  - `--numa numactl` — dual-socket optimization
  - `--poll-batch 1`
- Both services restarted and confirmed active

## Post-Opt Benchmark (17:56 UTC)
- **Node1:** 53.4 tok/s gen, 201.5 tok/s prompt | 2.42s total
- **Node2:** 53.2 tok/s gen, 182.0 tok/s prompt | 2.45s total
- **Result:** Generation speed unchanged (~53 tok/s on both)

## Node2 Single-GPU (19:18 UTC)
- Jeremy moved the second P6000 from node2 to node1
- node1 now has 2× P6000 (~53 tok/s), node2 has 1× P6000 (~43 tok/s)
- Everything working fine — expected performance difference

## Context Bloat Discovery & Fix (19:25-19:34 UTC)
- Jeremy noticed node1 server logs showing 18.29 tok/s — much slower than my 53 tok/s API benchmark
- **Root cause (per Jeremy's correction):** Slot had grown to ~76K live context. KV cache was already computed, but attending over 76K tokens slowed decode. Not re-processing — the slowdown is from attention cost growing with context length.
- API benchmark only measured generation time for new tokens, not the full context attending cost
- Server-side logs are the real source of truth
- **Fix:** Restarted llama-server to clear slot context
- **Post-restart:** Back to 53.7 tok/s
- **Diagnostics run:**
  - `curl -s http://node1:11434/slots` — slot ID 0, 262144 context, currently clean (149 tokens max)
  - `curl -s http://node1:11434/metrics` — prompt_tokens_seconds: 206.9, predicted_tokens_seconds: 53.7
- **Key metrics for future debugging:**
  - `llamacpp:prompt_tokens_total` / `llamacpp:prompt_seconds_total` — prompt processing
  - `llamacpp:predicted_tokens_seconds` — generation speed
  - `llamacpp:n_tokens_max` — largest observed context
- **Prevention config order (per Jeremy):**
  1. `--cache-idle-slots --kv-unified --cache-ram 8192` — auto-clear idle slots
  2. `--no-cache-prompt` — if (1) doesn't behave cleanly, sacrifice prefix reuse for predictable performance
  - For OpenClaw single-slot: `--no-cache-prompt` is the pragmatic choice. Each conversation is unrelated, so prefix reuse is useless. Predictable ~53 tok/s > rollercoaster from 18→53 tok/s.

## --no-cache-prompt Applied (19:42 UTC)
- Added `--no-cache-prompt` to BOTH node1 and node2 llama-server services
- Both restarted and confirmed active
- **Node1 benchmark (3 consecutive requests):**
  - Request 1: 53.5 tok/s gen, 199.9 tok/s prompt
  - Request 2: 53.3 tok/s gen, 238.3 tok/s prompt
  - Request 3: 53.4 tok/s gen, 257.2 tok/s prompt
  - **Stable — no degradation across requests**
- **Node2 benchmark:** 43.6 tok/s gen, 145.5 tok/s prompt (single request, consistent)
- **Metrics confirm no accumulation:**
  - Node1: `n_tokens_max=149` (stable), `prompt_tokens_total=90` (3×30 tokens)
  - Node2: `n_tokens_max=149`, `prompt_tokens_total=30` (1×30 tokens)
- **Verdict:** `--no-cache-prompt` working perfectly. Context no longer accumulates, performance stays flat.
  - `curl -s http://node1:11434/slots/0?action=erase` — clear slot without full restart
- Jeremy moved the second P6000 from node2 to node1
- node1 now has 2× P6000 (~53 tok/s), node2 has 1× P6000 (~43 tok/s)
- Everything working fine — expected performance difference
- **Why:** Model is GPU-bound — decode speed is intrinsic to 35B model on Ampere GPU. Tuning knobs improve consistency/latency, not raw throughput.

## Dell Removed from Network (18:00 UTC)
- Jeremy confirmed dell (RTX 6000 Blackwell) decommissioned
- Removed `dell` provider from openclaw.json
- Updated TOOLS.md: removed dell SSH alias, updated sub-agent model rule to use `pve3090-111`
- Updated IDENTITY.md: replaced dell reference with node1/node2/pve3090-111
- Provider list now: node1, node2, pve3090-111, ollama

## Key Decisions
- P6000 nodes (~53 tok/s) are adequate for batch/offline, not ideal for interactive
- pve3090-111 (~126 tok/s) is fastest available — route latency-sensitive tasks there
- Consider running smaller MoE model on P6000s for faster decode if needed

## Node2 Model Picker Fix (21:38 UTC)
- Request: add \`node2\` back to the selectable model list
- Root cause: active config at \`~/.openclaw/openclaw.json\` had \`models.providers.node2\`, but \`agents.defaults.models\` was missing \`node2/llamacpp\`
- Fix applied: added \`"node2/llamacpp": { "alias": "node2" }\` under \`agents.defaults.models\`
- Verification: JSON parsed successfully and confirmed \`node2/llamacpp\` now exists in the active config
- Note: Mission Control live API at \`http://127.0.0.1:4000/api/openclaw/models\` could not be checked because nothing is listening on port 4000
