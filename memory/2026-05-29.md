# 2026-05-29

## Changes Applied
- **All cron jobs** updated to use `ollama/qwen3.5:cloud` model (was `node1/llamacpp` or agent-default)
- **Config defaults** changed: `agents.defaults.model.primary` → `ollama/qwen3.5:cloud`
- **Main session model** changed: `pve3090-111/llamacpp` → `ollama/qwen3.5:cloud`
- Gateway restarted on claw after config changes

## Benchmarks
- **pve3090-111:** 9.83 tok/s (massive drop from usual ~100 — bottleneck suspected)
- **node2:** 62.89 tok/s (solid, 9/9 passed)

## Troubleshooting
- **dc (ssh localadmin@dc):** Was unreachable via SSH/Tailscale, came back with new IP `100.105.214.109`
- **DC WSL provider SSRF blocker:** OpenClaw's SSRF protection was blocking `http://wsl:11434` (resolves to `192.168.1.91`). Fixed by adding `"request": { "allowPrivateNetwork": true }` to the `wsl` provider config. Restarted gateway.

## Open Issues
- pve3090-111 speed degradation (9.83 tok/s vs ~100 expected) — investigate slot contention or GPU issue
